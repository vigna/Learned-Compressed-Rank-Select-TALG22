[package]
name = "sux-bench"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sux-bench"
path = "src/main.rs"

[dependencies]
sux = { path = "../../sux-rs" }
mem_dbg = "0.4"
rand = { version = "0.8", features = ["small_rng"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
